# Roadmap

本路线图只记录有限且明确的方向。除非单独提出任务，否则不在当前 PoC 迁移中实现这些内容。

## 近期

1. 基础 GUI 产品化。
2. 一键创建、关闭和显示 Child Session。
3. 配置并启动 MaaNOP、MFAA 和游戏。

## 中期

4. GUI 读取 MaaNOP `interface.json`。
5. 在主桌面选择任务和参数。
6. 实现 Child Session Worker 与 IPC。
7. 展示日志、状态和进度。

## 长期

8. 直接集成 MaaFramework，逐步替代 MFAAvalonia。
