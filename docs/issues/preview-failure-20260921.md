# 2026-09-21 登录识别失败与 Preview 恢复延迟

状态：最小化恢复已修复，用户实测反馈“没有太大的问题”并要求提交；失败时清空本轮用户未再次复现。

## 后续：最小化暂停并保留实例

用户反馈首轮 GUI 查询优化后恢复仍约 5 秒，失败时消失未再次复现。
本轮使用真实 v5.12.3 FramePool 和 WorkerPreviewService、静态 WPF 目标窗口测量：
首次订阅到首帧 4434 ms，再次订阅 4273 ms；其中 Controller Open 为 2399/2080 ms，
第一张 Capture 为 2014/2005 ms，而共享 buffer 仅 50/180 ms 就绪。
最小化撤销订阅后会销毁 Controller，再次打开重复原生初始化及截图等待。
固定版本源码的 `Win32ControlUnitMgr::speed_test` 连接时执行两次截图，
`FramePoolScreencap::screencap` 会等待新帧最多 2000 ms；静态画面因此尤其明显。

用户明确选择“最小化时暂停采集但保留实例”。现最小化时保留订阅并发送 `paused=true`，
停止新截图和 GUI 帧复制/显示，已在途调用可正常结束；恢复立即取消暂停并复用有效实例。
暂停仍每 2 秒续订，Worker 继续检查窗口身份；窗口失效清空旧帧并释放，恢复时再检查目标。
GUI 恢复时先消费保留的有效帧，再继续采集，不宣称该帧是在恢复后的瞬间重新截取。
隐藏到托盘、离开首页、收起预览、断线、租约过期和退出仍按原逻辑撤销及释放。

暂停属于控制请求新语义，协议升至 3，GUI/Worker 同步更新，旧版本继续按握手拒绝，
避免旧 Worker 忽略 `paused` 而继续截图；不改变像素格式、Run 状态或 Child Session 原生流程。

验证：暂停回归先失败，再验证停采/实例复用、暂停期间窗口关闭/重开及租约过期释放。
真实离屏 MainWindow + Pipe + WorkerPreviewService + FramePool 保持最小化 9 秒，
期间截图次数不增长，恢复显示 190 ms，Controller Open/订阅 start 均始终只有一次；
恢复后继续截图，隐藏窗口后正常释放。该测量在本桌面静态测试窗口进行，不冒充真实游戏分身复验。
GUI 另有最小化/恢复/隐藏的状态处理回归及控制管道暂停/恢复即时传送、暂停不向 UI 提交帧测试。

15:13 已备份并同步 GUI、Worker、两侧 Protocol DLL 和两份依赖清单（六文件），
产物保持 1.7.1 程序集版本，源/目标 SHA256 一致，三份 config/interface 文件未变。
备份在 `artifacts/preview-pause-backup-20260921-151304/`。
正式 self-contained 发布通过；Worker 完整自检仍在已有双进程完整帧测试超时，未标为整套通过。
目标目录完整 GUI 自检通过；针对目标 Worker 补跑 buffer/abandoned Mutex、暂停及窗口恢复、
真实协议入口与 Stop 隔离，以及全部非双进程 Worker 自检方法均通过。六个同步文件最终哈希再次一致。
用户随后确认实测“没有太大的问题”，同意提交本轮改动；未提供具体延迟采样，
不将该反馈扩展为全部故障边界或长期资源观察通过。提交前仅更新反馈记录、检查差异，不重复运行测试。

## 现场证据

- 检查目录为 `D:\MaaNOP-win-x86_64-v2.4.0`，实际 interface 版本为 MaaNOP v2.4.3。
- GUI 日志记录四次 `AccountClaims` 失败，最后一次为 14:39:58.536 提交、14:40:30.912 终结。
- 框架回调记录最后一次在 14:40:10.743 进入 `Login`，14:40:30.858 失败。
  期间 `ReChooseServer` 和 `RefreshPage` 连续模板识别失败，最后得分分别为 0.106961 和 0.266437。
- 对应模板分别是黄色“开始游戏”按钮和顶部“刷新”按钮；不是账号密码登录识别。
  当前日志没有当时截图，不能判定是页面、窗口尺寸/位置、模板或采集内容不匹配。
- 用户确认失败前停在登录界面，失败时预览出现“等待游戏画面”，随后恢复；最小化后恢复约需 3–5 秒。
- 四次运行使用同一 Worker、Session 20、游戏 PID 11580 和 HWND `0x2020C`；
  所检查 GUI 日志没有 RDP 断开、Worker 重启、Preview capture/transport 警告。
  日志没有正常预览状态切换记录，因此不能排除未记录的订阅/窗口变化。

## 已复现和修复的延迟

`LivePreviewClient` 收到不含 descriptor 的 Preparing 响应后，原先等待正常的 2 秒续订周期，
才再次获取已准备好的共享像素缓冲区；最小化撤销订阅后恢复也走同一路径。
现只在尚未取得 buffer 时按 100 ms 查询；取得后仍每 2 秒续订，帧采样和租约不变。

真实 Named Pipe + PreviewBuffer 回归先复现已就绪首帧仍等待 2006 ms；修改后首次及再次订阅
分别为 132 ms、136 ms，完整 GUI 自检通过。该数值仅表示缓冲区就绪到显示回调，
不包含真实游戏的原生 Controller 建立和截图时间，不承诺实机全部恢复时间为 100 ms。

## 失败时清空尚未复现

隔离 MainWindow + Pipe 检查向已显示预览的窗口发送 Running → Failed 终态，
两种环境 Ready 时序下均持续保留同一位图和订阅，观察覆盖 4.5 秒及续订。
这只排查 GUI 对普通 Failed 状态的处理，不覆盖真实 FramePool 双 Controller 清理或窗口变化。

下一次需要失败现场游戏截图及预览状态切换证据，才能进一步区分目标失效、订阅重建和原生截图行为。
本轮未重跑真实游戏任务、未修改 MaaNOP 资源或 Child Session 流程，未将隔离测试记为实机复验。
14:53 用户退出 GUI/Worker 后，已将保持 v1.7.1 程序集版本的 GUI DLL 同步至安装目录，
源/目标 SHA256 一致，三份 config/interface 文件哈希未变；仅替换主 GUI DLL。
旧 DLL 和部署时日志基线保存在 `artifacts/preview-restore-backup-20260921-145324/`。
目标目录完整 GUI 自检通过，首次/再次订阅首帧等待为 136/132 ms；实际最小化恢复及失败闪退待用户复验。
